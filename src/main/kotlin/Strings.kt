fun main () {

    // https://youtu.be/nTDqOuhHFkI

    var s : String = "Ahmed Saadi"
    var c = s.get(3) // Will print the third char on the string
    var c2 = s.length  // Print the length of the string
    println(c)
    println (c2)

    // Equals

    // Will print " True " if the st1 and st2 is the same output (text) return boolean
    var st1 : String = "Mody"
    var st2 : String = "Mody"

    var stEqual = st1.equals(st2)
    println(stEqual)

    // toUpperCase

    var name2 : String = "mohmmad khlifa"
    var sUpper = name2.toUpperCase()
    println(sUpper)



}